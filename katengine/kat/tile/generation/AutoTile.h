#pragma once

#include "kat/core.h"

namespace kat {


    // In opposition to the other systems in this folder, the auto tiler will not change the form of the tilemap, but instead will take a rough 'what am i' layout and use various edge, corner, and center textures to auto tile into smoother texture transitions
    class AutoTile {
    public:
    
    private:
    
    };
    
}